apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "picoclaw.fullname" . }}-test-connection"
  labels:
    {{- include "picoclaw.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "picoclaw.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
